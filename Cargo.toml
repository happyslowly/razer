[package]
name = "razer"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
hidapi = { version = "2", default-features = false, features = [
  "linux-native",
] }
